spring:
  artemis:
    mode: native          # native = connect to external broker
    host: localhost
    port: 61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false  # false = queue, true = topic